import { Injectable } from '@angular/core';

@Injectable()
export class TrafficAlertService {

    // List Alert
    static TrafficAlertListAdapter(data: any): any {
        return data;
    }

    // Create Alert
    static TrafficAlertCreateAdapter(data: any): any {
        return data;
    }

    // Edit Alert
    static TrafficAlertEditAdapter(data: any): any {
        return data;
    }

    // Delete Alert
    static TrafficAlertDeleteAdapter(data: any): any {
        return data;
    }
}
